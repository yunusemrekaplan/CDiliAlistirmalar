#include <stdio.h>
#include <math.h>
#define MAX 500

int main()
{
	int i,j,k,num,sum,dizi[MAX];
	i = 1;
	sum = 0;
	dizi[k];
	
	while(1)
	{
		for(j=1; j<=sum; j++)
		{
			if( sum % i == 0 )
			{
				dizi[k] = i;
				k++;
			}
		}
		sum = sum + i;	
		i++;
	}
}
